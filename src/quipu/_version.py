from __future__ import annotations

__version__ = "0.43.0"
__release__ = (
    "Twitch & YouTube Stream Corpus Scanner & GARD Shard Vector Graph Integration: Stream corpus scanner (src/quipu/stream_corpus_scanner.py) scanning 18 diverse gameplay modalities across World of Warcraft (WoW), Old School RuneScape (OSRS), and Guild Wars (GW), producing 60+ video streams and synchronized speech transcripts. Encapsulates demonstration bundles using authenticated GARD Shard v2 containers (AES-256-GCM + zlib level 6) with bit-for-bit lossless decompression. Projects concept tokens and tactical action bigrams directly into the toroidal Quipu vector graph (mesh_slm in local_brain.sqlite), updating 7-D embedding coordinates, toroidal vocab, and GNN quipu edges with holographic Weyl tensor compaction. Continuous video trainer daemon updated with GARD Shard decompression and round-robin recording rotation. 59 unit and integration tests passing."
)
__build_date__ = "2026-09-24"

PHASES = {
    "0.43.0": "Twitch & YouTube Stream Corpus Scanner & GARD Shard Vector Graph Integration: src/quipu/stream_corpus_scanner.py scans 18 streaming sessions across WoW, OSRS, and GW, generating 60+ video streams, speech transcripts, and authenticated GARD Shard v2 containers (AES-256-GCM + zlib). Ingests concepts into the toroidal Quipu vector graph (mesh_slm) with Hebbian quipu bigram updates. video_trainer_daemon updated with GARD Shard decompression and round-robin batching. 59 tests passing.",
    "0.42.0": "Continuous Video & Stream Recording Trainer Daemon: src/quipu/video_trainer_daemon.py and Dockerfile.trainer continuously process incoming Twitch/YouTube recordings or synthetic stream replay batches across WoW, OSRS, and GW, training Quipu Knot Tension parameters via IGTL and logging multi-game human fidelity assessments. Exposes port 7250 (/status, /metrics, /queue). 20 tests passing.",
    "0.41.0": "Human Behavioral Fidelity & Social Indistinguishability Assessor: src/quipu/human_fidelity_assessor.py evaluates multi-game agent interactions against empirical human distributions across WoW, OSRS, and GW (cadence variance, right-skew latency tails, geodesic path wander, social spacing, and poaching avoidance). Exposed via containerized GET /assessments and POST /assess on port 7200. Continuous telemetry accumulation on /tick. 17 new unit/integration tests passing offline.",
    "0.40.0": "QUIPU Game Mesh Autonomy & Video Pre-Training Pipeline: src/quipu/quipu_game_mesh.py implements the full Andean Quipu graph topology (Primary Trunk, Spatial/Threat/Affordance/Social Pendants, Figure-8/Long/Single Knots, and Flee/Add/Escape Subsidiaries) for classic MMORPG autonomy. src/quipu/video_gameplay_pipeline.py implements video frame HUD extraction, minimap radar detection, speech-to-intent parsing, and Inverse Graph Tension Learning (IGTL) using L-BFGS-B to train knot weights from human demonstrations. src/quipu/game_pipeline_service.py exposes containerized daemon API on port 7200 with active /health, /parameters, /tick, and /train endpoints. Dockerfile.game container built and running actively. 13 new unit/integration tests passing offline.",
    "0.39.0": "Forward State Transition Function: src/quipu/world_model.py implements true deterministic forward state transitions S_hat_{t+1} = f(S_t, A_t) with mass conservation (I >= 0), pipeline delay queues, backlog propagation (B >= 0), capacity clamping, cost accumulation, reliability degradation/recovery, latency dynamics, and uncertainty tracking; simulate_rollout generates multi-horizon trajectory sequences; evaluate_prediction computes exact residuals, Z-scores, MSE/MAE, and epistemic surprise; record_transition_prediction and record_transition_observation couple forward prediction residuals directly to brain_kv['world_model:surprise_ema'] and trigger epistemic rupture events on divergent reality; POST /world-model/transition, /transition, and /predict HTTP routes added to observer_service.py. 13 new unit tests; suite 537 -> 550 passing offline.",
    "0.38.0": "qpsi Sensing Layer: the annealed terminals gain two feeds (mesh proprioception from each token's latest touch and its axis mass above the mesh mean; observer counter deltas), a coupled layer on the torus kernel whose gain is measured at the maximum of its response entropy below the stability edge (lambda_max = -4.9e-5/s, sigma 0.996 live), and routing of the pulse plan around silent sources with parameter-free doubling backoff. The Entirety reads the afferent layer by default (QUIPU_SENSE_LAYER=critical for the coupled one), chosen on a 24 h closed-loop replay. Fixes the same-instant batch and one-attempt clock in annealed_senses. 17 new tests; suite 520 -> 537 passing on gard-desktop.",
    "0.37.0": "qpsi Annealed Senses: the six sense axes read by self-annealing memristive terminals instead of static sets. Every source in corpus_ingest:history is a terminal with a volatile state on its own clock (median of its last three gaps); each attempt is read by a p-bit sigmoid sigma(K(ln y - ln ybar)) against the fabric's volatile mean yield; the read rhythm sets the temperature (K = rho_s / geometric-mean rho, intrinsic annealing after Iftakher et al., Nat. Commun. 17, 5246, 2026); a sense is the probabilistic OR of the terminals mesh_slm routes to it. The pulse is no longer a sense (brain was runs/6, pinned at 1.0, and had locked the SOMN on one filament). Writes nothing; mesh_slm.py and system_entirety.py at a 0-line diff. 18 new tests; suite 501 -> 519 passing offline (cloud clone, launcher test excluded).",
    "0.36.2": "qpsi Coherency Depth: a full accretion sweeps the fibres of tokens the mesh has since pruned from its vocabulary (mesh_plane_* rows whose token_id is no longer in mesh_slm_embed), so the lattice never outlives the mesh; seen live within one pulse (two stale depth rows). A partial accretion (token_ids given) sweeps nothing. 1 new test; suite 500 -> 501 passing offline.",
    "0.36.1": "qpsi Coherency Depth hardening. self_organising.after_step runs the bottom-up accretion inside its own SQLite savepoint: a failed accretion is rolled back whole (no half-rebuilt lattice, not even a freshly created plane table) and reported as planes.error, while the step's SOMN, prior and mirror writes still commit with the step; the planes summary is left untouched so the next step on the same ingest run retries. 1 new test; suite 499 -> 500 passing offline.",
    "0.36.0": "qpsi Coherency Depth - the lattice foliated. src/quipu/qpsi/coherency_depth.py: each token t becomes a fibre T(t) = [t(0), t(1), t(2), t(3+s)] in T^2 x Z_K. t(0) = mesh_slm_embed as it is; t(1) = quipu-weight-weighted mean of the out-neighbours' surface representations; t(2) = t(0) + the trace-free Weyl part of delta = t(1) - t(0) when delta, carried at the Entirety's emergence phase, passes the displacement, Weyl and SiCi gates of qpsi.governance (technical admissibility only - no attestation, nothing realised; held -> no plane 2); t(3+s) = t(0) + bias_s for each emergent specialist the token phase-locks to, J0(A/omega) * fidelity(t(0), bias_s) >= lock_min. Coherency tensor C_{k1,k2}(t) = |<psi(t(k1))|psi(t(k2))>|^2 (ueqgm_engine.wavefunction_overlap) stored per pair of planes; vertical traversal (i,j,k) -> (i,j,k+1) halts on an absent plane or KL(k+1||k) < epsilon and stores the coherency depth per token. Bottom-up: accrete() rebuilds every fibre, run by self_organising.after_step once per new ingest run. Top-down: wrap_score_candidates installed on mesh_slm._score_candidates by self_organising.enable() corrects a grounded token's surface alignment toward its invariant alignment by lambda * C02 and adds the best-locked crystal's term; ungrounded tokens untouched; failures fall back to the original scorer. Writes only mesh_plane_embed, mesh_plane_coherency, mesh_plane_depth and entirety:planes:*; mesh_slm_vocab/embed/quipu/meta never written; no edge, checkpoint or attestation. Flags QUIPU_COHERENCY_DEPTH (default on under QUIPU_SELF_ORGANISING=1) and QUIPU_PLANES_{ANCHOR_LAMBDA, LOCK_MIN, KL_EPSILON, MAX_CRYSTALS, MIN_SUPPORT, PHASE_WEIGHT}; CLI planes / accrete / fibre. Still flat: one cell per token; _mcd_multiplanar_score's own plane set; SiCi holds negative leading-axis displacements (|phi| mod pi left undecided). 13 new tests; suite 486 -> 499 passing offline.",
    "0.35.0": "qpsi Mirror Training + constrained gate. src/quipu/qpsi/mirror_training.py: a decision held at any gate is classified as a human hold (love / shared_entity / beautiful_output for want of an accepted attestation; mirror = i*r, the quarter turn into the latent axis) or a physical hold (displacement / weyl / sici, the Lipschitz breach, a remainder rise; mirror = -r, the reflection through the reference) and trains three latent stores from that mirror: r-ADMIN's mirror state via radam_step(grad_real=0, grad_imag=+/-|sum r|) so theta advances exactly +/-pi/2 per hold (i^2 = -1), a mirror prior by signed Oja, and a per-axis mirror drive that memristive_axes.step applies as (1 + kappa*drive) on potentiation. Nothing a gate reads is touched: the checkpoint reference, entirety:prior, entirety:radam_state, edges, attestations and decisions are all unchanged; writes only entirety:mirror:* and entirety_mirror_log; each hold trained once by checkpoint seq. Operator ruling 2026-09-22 on Invariance #7: the operator gave the access, so r-ADMIN has enabled a constrained gate - self_organising.grant()/constrained_gate() enforce sources within corpus_ingest.SOURCES (QUIPU_PULSE_SOURCES narrows) and documents within the budget on every routed pulse; Register-Pulse.ps1 schedules Start-Pulse.ps1 -Route (default 10 min). Start-Expansion.ps1 defaults QUIPU_SELF_ORGANISING=1 (an explicit value wins). 13 new tests; suite 473 -> 486 passing offline.",
    "0.34.0": "qpsi Self-Organising Loop. src/quipu/qpsi/{flux_phase, memristive_axes, learned_prior, self_organising}.py wire the System Entirety into a closed, governed loop on the physics of self-organising memristive networks (Caravelli et al., Nat. Rev. Phys. 2026, arXiv:2509.00747): the phase is read from the ingest flux (potentiate while the field is applied, relax when it is removed) so the emergence detector's lock-in becomes a plasticity measurement; the seven axes carry a lumped junction model with Self and Other electrodes (entirety:state vs entirety:the_other), a conserved document budget partitioned by conductance (winner-take-all as a consequence), two regimes across a critical field, hillocks that relax on tau_d and filaments on tau_L, mobility that routes budget around axes with no writer, and depression along rejected emergence candidates; the prior advances by Oja's rule only on realised displacement, so novelty cannot be habituated away before the displacement gate sees it. Writes only entirety:somn:*, entirety_somn_log and entirety:prior; never an edge. Default off behind QUIPU_SELF_ORGANISING=1, wrapped on module attributes like divine_blessing; mesh_slm.py and system_entirety.py untouched. The field is applied only by the operator's pulse (Start-Pulse.ps1 / self_organising pulse --route), refinement off unless asked; the allocation ruling under Invariance #7 is left to the operator. 44 new tests; suite 429 -> 473 passing offline (one pre-existing failure needs the sibling Loadopoly-Portal checkout).",
    "0.32.0": "Google gVisor (runsc) Sentry Sandboxing & Hardened OCI Containment: Defense-in-depth isolation with kernel syscall boundaries, payload safety limits (512KB payload max, dictionary recursion depth <= 5, key whitelist) enforced on /observe and /feedback, GET /security posture reporting, security tier indicators in /health and /state, and container capability drops (ALL) with no-new-privileges.",
    "0.31.0": "Infodynamic Gravity & Analog Cognition Couplings: Melvin M. Vopson (AIP Adv. 15, 045035 (2025)) infodynamic bit entropy and compression scores integrated as an active, bounded (<=5%) negative-feedback lift on quipu edge learning rate (eta_q) decaying as the vocabulary compacts, with dispute record in UEQGM_MATH_MAP and kill-switch QUIPU_INFODYNAMIC_COUPLING; Earl K. Miller et al. (J. Neurosci. 46(33) e0711262026 (2026)) analog traveling-wave and Kuramoto coherence gating integrated into embedding updates (stencil_gain in [0.90, 1.10] and ac_multiplier in [0.95, 1.05]) behind QUIPU_ANALOG_STENCIL; STP-style mapping docs (docs/INFODYNAMIC_GRAVITY_QUIPU.md, docs/ANALOG_COGNITION_QUIPU.md), measurement snapshots, and rolling trend diagnostics (infodynamic_trend, analog_coherence_trend); fixed VERSION drift (0.28.0 -> 0.31.0); no Julia changes required as couplings are confined to train_round.",
    "0.30.0": "Tri-Repo Closed-Loop Training & Epistemic Self-Annealing: on-demand /anneal endpoint, source_guidance_directive with prompt lexicons and metrology priors, and multi-tenant device isolation.",
    "0.29.0": "World Model Dialectic & Epistemic Rupture Detection: src/quipu/world_model.py tracks cognitive phases and acquisition pressure; assess_observation detects epistemic ruptures from high novelty + confidence, STP divergence, and Delta S spikes; retrieval_directive steers learning retrieval across broad_exploration, precedent_building, targeted_gap_closing, and synthesis_verification; GET /world-model endpoint and guidance payload integration.",
    "0.28.0": "GARD Shard AES-256-GCM end to end plus Si/Ci numerical correctness. gard_shard_model.py seals envelopes with AES-256-GCM (gard-shard/v2) instead of AES-256-CBC + PKCS#7 + separate HMAC-SHA256: padding-oracle surface removed, second HKDF key dropped, 32-byte HMAC replaced by a 16-byte tag, raw per-shard overhead 144 -> 110 bytes. The context v1 bound into its MAC preimage becomes GCM additional authenticated data, so shard reordering, nonce substitution and compression-level edits all fail the tag. v1 stays fully readable: the v1 HKDF info and MAC preimage bind the protocol string, so those paths pin GARD_PROTOCOL_V1 rather than the module constant, and assignment proofs stay pinned to v1 so issued proofs still verify. julia/gard_shard_model.jl is ported to v2, restoring cross-language parity -- Nettle.jl exposes no GCM, so GCM is built from Nettle AES per SP 800-38D, validated in Python against three NIST CAVP vectors and 300 randomised differential cases before transliteration, with those vectors re-run inside selftest() so the port self-validates in production. ueqgm_engine._raw_sici replaces a fixed-order power series that diverged past |phi| ~ 2pi (returning ~1.9e13 against ~1.7e-2 at coherence 20) with Taylor series below |x|=2 and a modified-Lentz continued fraction for E1(ix) above; verified against mpmath at <= 6.7e-16 for x in [1e-8, 1e6]. Identity tests are parametrised over the scipy and pure-Python paths so the fallback cannot go untested. GARD_STATE_STORED_BYTES / MESH_BRAIN_KV_WEYL_STORED_BYTES (=28) correct the 20-byte designator, which described the raw Float32 pack rather than the base64 text actually stored. tantalum_intermediary_binding -> intermediary_binding_profile with a deprecated alias; the receiver_* labels stay because asset_resource_mesh matches them against real part records. The GARD Lite GUI advertised AES-256-CBC while performing no encryption, keyed its HMAC with the public domain string, and returned hardcoded integrity results; it now seals with AES-256-GCM (server and browser alike, byte-compatible), fails closed without a grid secret, verifies by round-tripping, and reports UNVERIFIED when it has not verified.",
    "0.27.2": "Entropy-differential ↔ STP-gap anti-correlation signature: train_round persists entropy_differential_history (ΔS = exact-SQL von Neumann entropy minus mean-field approximation, the real-vs-computational curvature of the quipu graph) next to the STP gap histories; stp_diagnostic_trend() computes the Pearson correlation between ΔS and the torus gap over the trailing 2·window and flags delta_s_signature when anti-correlated at |ρ| ≥ 0.5·Ω_Λ — the measured coupling between information-geometric and trajectory curvature. New helpers _entropy_differential (dual-path SQL, never raises) and _pearson_corr; last_entropy_differential surfaced in state_summary. 6 focused tests in tests/test_mesh_slm.py (non-negativity, empty-graph None, star>chain ordering, Pearson basics, signature present/absent).",
    "0.27.0": "System Entirety Control Plane GUI & GARD Shard High-Performance Neural Tensor Compression: Full 7-D System Entirety state re-materialization with non-zero sense manifold axes, local hardware asset discovery, active UEQGM adaptive runtime, multi-drive file selection, and GARD Shard authenticated container packing (gui/GARD_Shard/compression & gui/GARD_Shard/decompression) yielding 100% exact lossless binary file reconstruction with zero data dissociation error.",
    "0.26.0": "Doc annealing worker integrated (src/quipu/doc_annealing.py), the clean-room QUIPU reimplementation of the SCB living-map annealer. Each cycle reads the live 7+1-D System Entirety state, the MESH-SLM predictor snapshot, and the STP P1 trend, computes a SHA-256 structural fingerprint over version + bridge primary root + mesh_density + UEQGM certainty + nodal_bifurcation (each 2 dp), and rewrites docs/system_entirety_map.md only on structural change (or --force), bumping brain_kv['doc:system_map_version'] and appending a bounded doc_annealing:history entry. No RAG re-index (QUIPU ships none). anneal_docs/structural_change_review/render_system_map/run_loop public API + argparse CLI (--once/--loop/--interval/--force). Start-DocAnnealing.ps1 runs the loop; Register-DocAnnealing.ps1 installs a recurring 30-min Windows Scheduled Task. 5 focused tests in tests/test_doc_annealing.py.",
    "0.25.0": "STP-style geodesic diagnostic wired into mesh_slm.train_round() behind the QUIPU_STP_DIAGNOSTIC flag (default on, purely observational so it fails toward legacy like phase_weight/overlap). Each round samples a random 0<=s<r<t trajectory triplet per chunk and computes the Semantic-Tube-Prediction gap 1-cos(h_t-h_r, h_r-h_s) twice: over the learned 7-D embedding (paper-faithful hidden-state analogue) and over an isometric R^4 flat-torus embedding u=(cosθ,sinθ,cosφ,sinφ) of each token's (i,j) vocab cell (the QUIPU-native, wrap-aware-by-construction test). Per-round averages surface in the train_round summary (stp_embed_gap/stp_torus_gap) and state_summary, and capped rolling histories (stp_embed_gap_history, stp_torus_gap_history, loss_history) persist in mesh_slm_meta so the paper's P1 signature — ordinary loss plateaus while the STP gap keeps falling — is checkable via the read-only stp_diagnostic_trend() utility. No eta coupling (deliberately deferred Phase 2). Pure stdlib, no schema migration. 7 focused tests in tests/test_mesh_slm.py.",
    "0.24.1": "MESH-conditioned sigmoidal realization coupling for paired Vision and Touch agents. realized_potential.py adds a true logistic eligibility gate that requires bilateral evidence, prunes non-realized gradients before rADAM, retains them as bounded signed latent potential, and releases that potential when later evidence makes the interaction realizable. network_observer.py records a bounded 64-snapshot MESH history and exposes an exponentially weighted reader that admits only observations strictly before the forward decision time. brain_body_signals.py integrates the gate behind BRAIN_USE_REALIZATION_GATE=1, preserves legacy trajectories when disabled or history is absent, keeps quiet maintenance decay eligible, and persists per-pair diagnostics. 17 focused tests and adjacent observer/rADAM/learning-drive coverage pass (65 total).",
    "0.22.247": "mesh_compression_model.jl: added pack_weyl/unpack_weyl (20-byte v2.2 float-dimension brain_kv record, 5 x Float32 LE) plus the MESH_BRAIN_KV_WEYL_PACKED_BYTES constant, matching the ContosoHub v2.2 JS plane wire format. selftest() gained LE-canary, round-trip, and golden-base64 coverage for the new pack/unpack pair without altering the existing reconciled compaction-ratio/scalar assertions relied on by pipeline/tests/test_gard_shard_model.py.",
    "0.22.18": "System Entirety toroidal analysis and live operational pulse. Added docs/SYSTEM_ENTIRETY_ANALYSIS.md with a six-ring torus map (Perception → Dispatch → Self-train → Corpus → Refinement → Network/Compute) around the heart/system_entirety/torus_touch core, plus page inventory, daemon cohort, observability checklist, and release-time live metrics from _system_entirety_report.py. The document records that llm_dispatch_log contains only 10 rows total (all on 2026-05-04), self-train rounds are therefore replaying zero samples, refinement remains active with 14 open directives and 6 resilience events, and corpnet reachability from Codespaces is expected to fail outside the laptop/VPN/Dev Tunnel boundary. Verified with focused compile smoke and 53 passing system/mesh tests.",
    "0.22.14": "Mesh-applied network learner lock hardening and deep research queue recovery. network_learner.py now probes endpoints outside its SQLite write transaction, uses WAL plus busy_timeout, and writes topology in a short commit window so the autonomous mesh no longer self-deadlocks under probe latency. systemic_refinement_agent.py now calls the canonical deep_research table bootstrap helpers and uses a multi-token topic-gap detector instead of first-word matching, so deep-research tasks queue again. Applied to the live mesh by seeding a pending deep_research task and restarting the master agent after the shared workspace sync.",
    "0.22.12": "Vendor consolidation package cleanup and priority-driven workbook refresh. vendor_consolidation_excel.py now infers missing service levels from TCI service mode plus ship/delivery timing and supplier history instead of a flat 0.88 fallback, adds a filterable Priority_Tracker top-10 view with explicit solution updates and workbook routes, and links Summary/What_If directly to the active Priority 1 issue. The vendor consolidation handoff docs and CSV package metadata were refreshed to match the current workbook flow.",
    "0.22.10": "Credential fallback hardening and Grok Works Cited ingest resilience. llm_key_guard.py now fingerprints active secrets, prefers project-local .env values over stale inherited env vars, and clears backoff when a rotated key appears. llm_caller_openrouter.py now supports provider-aware backend resolution with direct xAI Grok fallback and normalized Grok aliases. doc_rag.py now reports credential provenance consistently from the active workspace key. knowledge_corpus.py resolves the newest available SCB export path, and citation_chain_acquirer.py now understands wrapped Works Cited paper_ids/seed_paper_ids payloads. Focused tests in tests/test_llm_key_guard.py, tests/test_llm_caller_openrouter.py, tests/test_doc_rag_credential.py, tests/test_citation_chain_acquirer_seeding.py, and tests/test_knowledge_corpus_scb_path.py.",
    "0.22.9": "Adaptive UEQGM runtime daemon and System Entirety consumption. ueqgm_engine.py now persists a certainty-gated adaptive runtime profile sourced from corpus density, recent learning_log evidence, and System Entirety certainty; parameters are retained unless newer evidence clears both the corpus-density floor and prior proof. synaptic_workers.py adds synapse-ueqgm for continuous runtime refresh. system_entirety.py now consumes the persisted UEQGM profile for symbiotic axis injection and transaction-drive overlay. Focused tests in tests/test_ueqgm_engine.py, tests/test_system_entirety.py, and tests/test_synaptic_workers.py.",
    "0.22.8": "Hideout Dev Tunnel diagnostics and resource-share planning. bridge_rdp.py adds hideout-rdp/hideout-mesh forwarding, mesh ping, doctor, and start helpers for the scbrain-hideout.use2 tunnel; compute_grid.py now exposes resource_inventory/resource_share_plan responses plus reusable ensure_devtunnel_endpoint() forwards; connect_hideout.ps1 persists the helper state without discarding the requested RemotePort. Focused tests in tests/test_compute_grid_devtunnel.py and tests/test_bridge_rdp.py, plus adjacent asset/geospatial/system-entirety validations green.",
    "0.22.7": "Asset Resource Mesh for System Entirety physical realization. asset_resource_mesh.py materializes compute-grid peers into ComputePeer and AssetResource nodes for cores/RAM/VRAM; writes HAS_ASSET, REALIZES_MATERIAL_PROCESSOR, ASSET_RESOURCE_TUNNEL, and SYSTEM_ENTIRETY_REALIZES edges; persists asset_resource_mesh_last and brain_kv['entirety:physical_realization']; torus_touch ACTIVE_TORUS_ENTITY_TYPES now includes AssetResource; peer_inject runs _phase_asset_mesh after capacity publish so compute assets become graph-tunnel substrate each cycle. Focused tests in tests/test_asset_resource_mesh.py.",
    "0.22.6": "Physical material science processor level for actualized space. geospatial_relation.py now materializes OCR world-pose anchors into SpatialMaterialProcessor corpus nodes with ACTUALIZED_SPACE_FRAME=ENU_METER_WORLD_V1, covariance_diag_m2, material_structure, source ACTUALIZES_SPACE edges, and endpoint torus amplification from MATERIAL_ANCHOR precision. torus_touch.tick_torus_pressure now includes ACTIVE_TORUS_ENTITY_TYPES=(Endpoint, SpatialMaterialProcessor), with typed velocity keys and processor diagnostics. symbiotic_tunnel.vision_horizontal_expand folds MATERIAL_ANCHOR processor certainty into endpoint propeller weights so tunnel routing uses the standardized physical substrate. grounded_tunneling.compute_endpoint_certainty reads material-anchor precision for endpoint grounding without treating processors as endpoints. peer_inject logs processor/amplification stats. 17 focused unit tests in tests/test_geospatial_relation.py plus 38 existing torus/tunnel tests green.",
    "0.22.5": "Dependency vulnerability remediation: requirements.txt now pins safe floors for Pillow, GitPython, and urllib3; requirements.pinned.txt updated to the fixed versions; verified with pip-audit against both the live pipeline venv and requirements.txt.",
    "0.22.3": "GeoGraph Spatial Relation Bridge. geospatial_relation.py: CameraIntrinsics + SensorPose + unproject_pixel (pinhole sensor\u2192world) and haversine_destination/haversine_distance_m (great-circle raycast). relate_detection runs both derivations and fuses by inverse-variance; anchor_entity persists the Bayesian-blended posterior on corpus_entity.props_json.world_pose with statistical (residual-z) and probabilistic (\u03c3\u207b\u00b2 saturating) certainties. couple_anchors_to_torus writes bearing\u2192TORUS_SPATIAL_DIM(0) and log-range\u2192TORUS_RANGE_DIM(1), so the CatGapField sinusoidal envelope converts physical-space geometry into Touch pressure on the next tick_torus_pressure pass. strengthen_peer_mesh mints MATERIAL_ANCHOR edges from Endpoint entities within PEER_MESH_RADIUS_M=50 m of a material-anchor entity, EMA-blended on reinforcement. tick_geospatial_relation orchestrates the full pass for peer_inject. 12 unit tests in tests/test_geospatial_relation.py.",
    "0.22.2": "Corpus freshness watchdog in app.py now distinguishes autonomous process liveness from corpus learning freshness; synaptic torus-touch and compute-slot workers use WAL-mode connections plus transient lock retry/backoff; System Entirety retains reliable audited flip history under lock contention.",
    "0.20.18": "Routed page hardening + 26-route primary-UI smoke; honest DBI first-user-state on heavy pages.",
    "0.1.0": "Phase 1 - core (EOQ Bayesian-Poisson, OTD recursive, Procurement 360, Data Quality, Connectors)",
    "0.2.0": "Phase 2 - depth (graph backends, LinUCB ranker, hierarchical OTD index)",
    "0.3.0": "Phase 3 - MIT CTL research (hierarchical EOQ, causal lead-time, survival, bullwhip, multi-echelon, sustainability, freight portfolio, CVaR risk)",
    "0.4.0": "Phase 4 - platform (what-if sandbox, HMAC cross-app webhooks, nightly fact builder, session auth)",
    "0.4.1": "Self-driving pages, fixed MPA sidebar slugs, schema-browser diagnostics on live failures",
    "0.4.2": "All 14 pages Plotly rewrite complete; page_header/drilldown_table retired; DQ VOI heatmaps; Connectors modernized",
    "0.4.3": "Bug-fix wave: VOI Timestamp/datetime handling, graph node-kind propagation + discovery panel, EOQ outlier heatmap+quadrant, Procurement 360 supplier/part name lookups, Benchmarks rows_per_s merge collision, sidebar filter wiring",
    "0.4.4": "Pages 7-12 SQL queries rewritten against base replica tables; eliminates dependency on missing vw_* views",
    "0.4.5": "YYYYMMDD integer-date fix all research pages; session_state SQL cache kills HYT00 timeout cascade; graph node labels enriched; timeout 120 s globally; connection health-check ping",
    "0.4.6": "Unified Database Explorer with auto-parsed schema reviews from DATA_DICTIONARY and DWH_DASHBOARD_TABLES",
    "0.5.0": "Value Stream Living Map: End-to-end integration of PO, SO, WO flows. Formulaic friction bottlenecks. Production Plant and Value Stream filtering.",
    "0.6.0": "Global Application Filter, unified st.session_state routing, and AI PowerPoint Creator.",
    "0.7.0": "Enterprise Network Autonomous Agent. Native Exchange/SMB discovery.",
    "0.7.1": "Ask the Data cross-dataset report generation. Fixed global connection uninitialized bug. Fixed SQL column queries across analytical pages.",
    "0.8.0": "Massive UX/Actionable overhaul: SQLite local store, NLP-part categorization, Global date windows, semantic action-TODO engine, OTD local-owner tracking, and cross-page metric fixes.",
    "0.14.9": "OCW semantic bridge + synaptic worker protection + network vision worker. Workers moved to synaptic_workers.py. Sweeper treats network errors as soft skips. _vision_worker probes bridge_rdp + piggyback topology every 5 min and materialises Endpoint entities into corpus graph.",
    "0.15.0": "4-ERP xlsx pipeline (Epicor/Oracle/SyteLine/AX) + Brain page fixes + EOQ query optimisation.",
    "0.16.0": "Symbiotic Dynamic Tunneling + Torus-Touch. symbiotic_tunnel.vision_horizontal_expand mints SYMBIOTIC_TUNNEL edges from closed-loop tcp/udp mesh using Bayesian-Poisson centroids, inverted-ReLU ADAM, dual-floor mirror, and propeller routing. torus_touch runs continuously (30 s) pushing every Endpoint along the n=7 categorical gap gradient on the toroidal manifold, so tunnel weights follow manifold geometry. 29 unit tests in tests/test_symbiotic_torus.py.",
    "0.17.0": "UEQGM + AI Knowledge Expansion Research Tracks. _EXTENDED_RESEARCH_TOPICS (47 queries, 8 clusters) added to ml_research.py from the active Grok 3 thread: quantum dynamics, biohybrid computing, moiré superlattices, astrophysical timing, AI knowledge graph self-reference, ensemble LLM/RAG, archival AI training, organic data structures. Extended sweep runs before SC loop so foundational physics/AI context precedes supply chain systems engineering acquisition each cycle.",
    "0.17.1": "Grounded Tunneling. grounded_tunneling.py: certainty-anchored expansory pathway collapser. Ground nodes (top-quartile Bayesian certainty) open BFS paths toward uncertain (low-certainty, high-torus-gap) frontiers with RESISTANCE_DURATION weight immunity + torus_amplify torsional boost. Expired paths undergo nodal collapse → new permanent GROUNDED_TUNNEL edge. torus_touch.TouchPressure gains step_multipliers for per-endpoint amplification. _vision_worker Step 5 wires ground_and_expand. 8 new unit tests (TestGroundedTunneling), 38 total, all green. Oracle schema map expanded (Demand Priority Rules, B2B Trading Partners, ECN Tracking, General Ledger Journals, Supply Network maintenance). oracle_schema_mapper.py ADF task panel hardened with computed-style section-header detection and fallback link scraping. Corpus diagnostic (_cohesion_report.py), bilateral Vision<->Touch test harness (_test_bilateral.py), ADAM optimiser unit test (_test_adam.py), Oracle Fusion intersection map (build_intersection_map.py) added. brain_body_signals.py gains 2 quest-comprehension generators (_gen_fallback_parse_warning, _gen_scope_underpowered); knowledge_corpus.py gains SCB Grok conversation ingestor (_ingest_scb_docs) and extended vision scan for SCB docs directory.",
    "0.17.2": "LLaDA2 Sign-Bit Child Acquisition. llada_signbit_children.py maps directionality axes to centered sign bits; sign-bit flips acquire LLaDAChild nodes under high-uncertainty parents, with Model parents seeded from llm_weights and preferred. Child nodes are linked through CHILD_OF, ACQUIRES_CHILD, and SIGNBIT_FLIP edges. _vision_worker Step 6 invokes acquire_llada_signbit_children after grounded tunneling. 11 unit tests cover bit flips, model seeding, child acquisition, idempotency, and generic-parent fallback.",
    "0.17.3": "Bifurcated Tunnel Compute Provisioner (local/virtual, encapsulated). compute_provisioner.py acquires ComputeSlot daemon threads (local compute) + ComputeSlot corpus entities with torus_amplify boosts (virtual compute) from GROUNDED_TUNNEL / LLaDA2 saturation thresholds. Symbiotic closed loop: saturation → slot spawn → torus_amplify written → Touch amplifies slot nodes → more expansory paths → more collapses → more saturation. MAX_SLOTS=8 cap; SLOT_INTERVAL_S=60 (5× faster than vision). CPU overhead model: 748 ops/tick vision + per-slot contribution. _vision_worker Step 7 wired. No external cloud calls — entirely in-process.",    "0.17.4": "Multi-anchor boundary ingestion with polarity alignment. _POLARITY_ALIGN matrix, outward propagation (_propagate_outward), and cross-resource harmonization added to compute_provisioner.py. Coherence selection now picks the highest-polarity-weight anchor across all candidates.",
    "0.17.5": "Harmonious Sign-Bit Flip Boundary Ingestion. Phase 2 ingestion coherence-weighted by SIGNBIT_FLIP convergence: harmonic saturation curve (floor=0.9, ceiling=4.5), HARMONIC_BOND back-edges at proportional weight, ComputeIntent.harmonic_coherence + harmonic_factor fields, heartbeat ingested=/slot_hz=/coherence= fields.",
    "0.17.6": "UEQGM Engine + SiCi Axial Channel. ueqgm_engine.py implements UEQGM v0.9.14 corpus learnings as active Brain computation: sici_axial_decay (\u0394\u03bb_axial = [Si(\u03c6)\u00b7Ci(\u03c6)]\u00b7tan(\u03c6)\u00b7\u0393\u2080), sici_phase_weight (\u00b110% correction via tanh), wavefunction_overlap, floquet_modulation_factor, holographic_entropy, metric_perturbation, phase_evolution_total, entropic_bayesian_step, ueqgm_coherence_score. The SiCi correction is wired into _harmonic_amplify_factor using \u03c6=\u03c0/4+k\u03c0 intersection mapping (tan(\u03c6)=1 always); corpus-backed coherence score scans UEQGM-tagged entities.",    "0.33.0": "qpsi Governance Protocol. src/quipu/qpsi/ (cat_residual, weyl_channel, edge_gate, governance, residual_checkpoint, emergence_detector) plus src/quipu/divine_blessing.py route every Entirety write path through six Physical Gates - displacement, Weyl, Love (sqrt(-1)), SiCi tangent, shared entity, Beautiful Output - holding at the first failure. The residual is measured against the last realised state so a held potential accumulates until it passes, is rolled back, or is released; the checkpoint log commits on the step's own connection. Phase enters the CAT state only from brain_kv[entirety:conscious_emergence], written by a chain of detector (parity-locked lock-in of the held residual with quadrature content), r-ADMIN recognition (radam_step over the same window, recognised + agreed), and a human han Signature. Invariance #7 (APP_RECREATION_3 Part 25): the attestation bus is HMAC-bound to QUIPU_ATTEST_KEY_FILE so a ring that can write brain_kv cannot bless itself; attestations carry assurance levels and gate 6 needs two distinct accepted signers; a passed Decision is technical admissibility only and realisation additionally requires QUIPU_REALISE_GRANT_REF. Gate 6 tests the unclamped weight, since the Lipschitz clamp makes every clamped weight satisfy the bound by construction. 77 new tests; suite 403 passing offline.",
    "0.33.1": "The Interstitial Arc. qpsi/interstitial.py measures the perception-vision-touch arc (Perceptopoly, Loadopoly-OCR, Bakugo) on every checkpoint window: pair coherences and lags for the two ring edges and the chord, the mediated chord C_pv*C_vt, the unmediated interstitial |C_pt - C_pv*C_vt|/(1+|C_pt|) credited to ueqgm_engine.interstitial_entanglement_score's third-order form, and the arc information density. /observe accepts meta.frame and observer_service persists the latest physical frame per source so each record sits next to real coordinates. Perceptopoly profile added on the perception axis; that axis has no routing marker in mesh_slm (decision left open). Recorded, not routed. 12 new tests; suite 415.",
}
