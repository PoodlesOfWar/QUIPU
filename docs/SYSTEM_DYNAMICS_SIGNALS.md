# System Dynamics Equations and Signals

Version: 0.31.0  
Date: 2026-08-22  

## Purpose

This document is the compact observability companion to [SYSTEM_DYNAMICS.md](SYSTEM_DYNAMICS.md). It focuses on equations, bounded control terms, and measurable signals that expose the dynamical state of the QUIPU Entirety and its tri-repo fleet (`QUIPU`, `Loadopoly-OCR`, `Bakugo`).

---

## 1. State Vector & Manifold Surfaces

The primary state vector is generated by `system_entirety_state()` in [src/quipu/system_entirety.py](../src/quipu/system_entirety.py):

| Surface | Fields | Meaning |
| :--- | :--- | :--- |
| `axes` | `vision`, `touch`, `smell`, `body`, `brain`, `perception` | Active six-sense amplitudes after all additive injections |
| `observer` | scalar | 7th-dimensional tangent orthogonal to the six-sense hyperplane |
| `magnitude` | scalar | Euclidean norm of the active 7-D state $\|\vec{\Psi}_{7D}\|_2$ |
| `material_bifurcation` | density and topology fields | Degree of physical realization and mesh bifurcation |
| `pim_planning` | planning metrics and axis injection | Planning-driven dynamical injection |
| `repository_catalog` | catalog metrics and axis injection | Repository-coverage and modular-expansion signal |
| `ueqgm_runtime` | adaptive runtime profile | Certainty-gated symbiotic overlay |
| `transaction` | drive, bit-flip, topology | Realized motion of the current state |

---

## 2. Core Control Equations

### 2.1. Learning Acquisition Drive
Defined in [src/quipu/learning_drive.py](../src/quipu/learning_drive.py):
$$acquisition\_drive = \text{clamp}\Big(0.20 \cdot (1-s) \cdot d + 0.10 \cdot (1-v), 0.0, 1.0\Big)$$
- $s$: Corpus saturation $[0, 1]$
- $d$: Task difficulty $[0, 1]$
- $v$: Learning velocity $[0, 1]$

### 2.2. Observer EMA & Calibration
Calculated in [src/quipu/observer_service.py](../src/quipu/observer_service.py):
$$EMA_{conf}(t) = 0.90 \cdot EMA_{conf}(t-1) + 0.10 \cdot c_t$$
$$suggested\_min\_confidence = \max\Big(0.35, \min\big(0.90, EMA_{conf} - 0.15\big)\Big)$$

### 2.3. Cross-Corpus Frequency Prior (Touch Axis)
Calculated in [src/quipu/observer_service.py](../src/quipu/observer_service.py) and consumed in Bakugo:
$$P_{mesh}(t) = \frac{\text{freq}_{mesh}(t)}{\sum_{t' \in \mathcal{V}_{num}} \text{freq}_{mesh}(t')}$$

### 2.4. UEQGM Runtime Scale
Defined in [src/quipu/system_entirety.py](../src/quipu/system_entirety.py):
$$runtime\_scale = G_{ueqgm} \cdot \text{clamp}\Big(0.50 \cdot symbiotic\_gain + 0.25 \cdot expansion\_pressure + 0.15 \cdot certainty + 0.10 \cdot relational\_depth, 0.0, 1.0\Big)$$

### 2.5. Symbiotic Drive
Defined in [src/quipu/system_entirety.py](../src/quipu/system_entirety.py):
$$symbiotic\_drive = \text{clamp}\Big(0.35 \cdot symbiotic\_gain + 0.25 \cdot expansion\_pressure + 0.20 \cdot mesh\_alignment + 0.10 \cdot observer\_alignment + 0.10 \cdot certainty, 0.0, 1.0\Big)$$

### 2.6. Transaction Drive
Defined in [src/quipu/system_entirety.py](../src/quipu/system_entirety.py):
$$transaction\_drive = 0.42 \cdot observer + 0.18 \cdot nodal\_bifurcation + 0.14 \cdot mesh\_density + 0.10 \cdot planning + 0.08 \cdot catalog + 0.16 \cdot symbiotic\_drive$$

### 2.7. Heart Complex Plane State
Defined in [src/quipu/heart.py](../src/quipu/heart.py):
$$z = expansion + i \cdot bifurcation$$
$$B = \frac{\langle |g_{im}| \rangle}{\langle |g_{re}| \rangle + \epsilon}$$

### 2.8. Interstitial Entanglement Witness
Defined in [src/quipu/gard_info.py](../src/quipu/gard_info.py) and [src/quipu/ueqgm_engine.py](../src/quipu/ueqgm_engine.py):
$$E = \frac{|C_{01} \cdot C_{12} - C_{02}|}{1 + |C_{02}|}$$
$$\mu_{ie} = 1 + 0.05 \cdot E$$

### 2.9. STP Geodesic Gap & Entropy Differential Anti-Correlation
Defined in [src/quipu/mesh_slm.py](../src/quipu/mesh_slm.py):
$$\text{Gap}_{STP} = 1 - \cos(h_t - h_r, h_r - h_s)$$
$$\Delta S = S_{exact} - S_{mean\_field}$$
$$\rho(\Delta S, \text{Gap}_{STP}) \le -0.5 \cdot \Omega_\Lambda \implies \text{delta\_s\_signature} = \text{true}$$

### 2.10. Infodynamic Gravity & Compression Lift (Vopson 2025)
Defined in [src/quipu/ueqgm_engine.py](../src/quipu/ueqgm_engine.py) and [src/quipu/mesh_slm.py](../src/quipu/mesh_slm.py):
$$H_{bit}(p) = -p \log_2(p) - (1-p) \log_2(1-p), \quad p = \frac{N_{occ}}{N_{cells}}$$
$$\text{compression} = 1 - \frac{H(X)}{\log_2(k)}$$
$$\mu_{ig} = 1.0 + 0.05 \cdot \text{clamp01}(1.0 - \text{compression})$$
$$\frac{d(\text{compression})}{dt} \ge 0 \land \frac{d(N_{occ})}{dt} > 0 \implies \text{second\_law\_signature} = \text{true}$$

### 2.11. Analog Traveling-Wave Coherence & Stencil Gating (Miller et al. 2026)
Defined in [src/quipu/ueqgm_engine.py](../src/quipu/ueqgm_engine.py) and [src/quipu/mesh_slm.py](../src/quipu/mesh_slm.py):
$$R = \frac{1}{N} \left|\sum_{j=1}^N e^{i \theta_j}\right| \in [0, 1]$$
$$\mu_{ac} = 1.0 + 0.05 \cdot \text{clamp}(2R - 1.0, -1.0, 1.0)$$
$$\text{stencil\_gain}(cell) = 0.90 + 0.20 \cdot \text{interaction\_gain}[cell]$$
$$\eta_{\text{eff}}(token) = \eta \cdot \text{stencil\_gain}(cell)$$

---

## 3. Measurable Signal Inventory

| Signal Name | Source | Type / Bounds | Primary Consumer |
| :--- | :--- | :--- | :--- |
| `axis:vision` | `Loadopoly-OCR` / `quipu` | $[0.0, 1.0]$ | `system_entirety` |
| `axis:touch` | `Bakugo` / `quipu` | $[0.0, 1.0]$ | `system_entirety` |
| `axis:observer` | `observer_service` | $[0.0, 1.0]$ | `system_entirety` / `mesh_slm` |
| `confidence_ema` | `observer_service` | $[0.0, 1.0]$ | `_calibration()` |
| `stp_embed_gap` | `mesh_slm` | $[0.0, 2.0]$ | `stp_diagnostic_trend()` |
| `stp_torus_gap` | `mesh_slm` | $[0.0, 2.0]$ | `stp_diagnostic_trend()` |
| `entropy_differential` | `mesh_slm` | $[0.0, \infty)$ | `stp_diagnostic_trend()` |
| `interstitial_entanglement`| `ueqgm_engine` | $[0.0, 1.0]$ | `mesh_slm.train_round()` |
| `infodynamic_compression` | `ueqgm_engine` | $[0.0, 1.0]$ | `mesh_slm.train_round()` |
| `analog_coherence` | `ueqgm_engine` | $[0.0, 1.0]$ | `mesh_slm.train_round()` |
| `stencil_gain_mean` | `mesh_slm` | $[0.90, 1.10]$ | `mesh_slm.train_round()` |
| `transaction_drive` | `system_entirety` | $[0.0, 1.0]$ | `self_realization_loop` |
| `structural_fingerprint` | `doc_annealing` | SHA-256 hex | `doc_annealing` map trigger |

---

## 4. Operational Telemetry Commands

```bash
# 1. Inspect QUIPU Observer health and mesh counts
curl -s http://127.0.0.1:7100/health | jq .

# 2. Inspect Mesh SLM state, STP gap, and entropy differential
curl -s http://127.0.0.1:7100/state | jq .

# 3. Pull Vision guidance for Loadopoly-OCR
curl -s "http://127.0.0.1:7100/guidance?source=loadopoly-ocr" | jq .lexicon[0:10]

# 4. Pull Touch numeric priors for Bakugo
curl -s "http://127.0.0.1:7100/guidance?source=bakugo" | jq .numeric_lexicon

# 5. Check Bakugo local observer status
curl -s http://127.0.0.1:8765/quipu | jq .
```